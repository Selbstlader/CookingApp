# 移除过时的配置项，使用新的配置名称
# auto-install-peers=true  # 已过时
# shamefully-hoist=true    # 已过时

# 如果需要类似功能，可以使用以下新配置（可选）
# install-strategy=hoisted
# auto-install-peers=true  # 在 pnpm 中仍然有效
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=true
